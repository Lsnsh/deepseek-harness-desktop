{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability: core window management for the bundled splash and the served GUI. The remote server origin does not get IPC access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
